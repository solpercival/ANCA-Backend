# ---- API ----
APP_ENV=local
LOG_LEVEL=INFO

# ---- OAuth2 / auth ----
JWT_SECRET=change-me-in-prod
JWT_ALGORITHM=HS256
ACCESS_TOKEN_TTL_MINUTES=60

# ---- Postgres (relational + pgvector) ----
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=rag
POSTGRES_USER=change_in_prod
POSTGRES_PASSWORD=change_in_prod

# ---- Redis (session + cache) ----
REDIS_HOST=redis
REDIS_PORT=6379

# ---- Model provider selection ----
EMBEDDING_PROVIDER=ollama
LLM_PROVIDER=ollama

# ---- Ollama ----
OLLAMA_BASE_URL=http://ollama:11434
EMBEDDING_MODEL=bge-m3:567m
LLM_MODEL=qwen3:4b

# ---- OpenAI-compatible providers ----
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_LLM_MODEL=gpt-4o-mini

# ---- Anthropic / Claude ----
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_API_KEY=
ANTHROPIC_LLM_MODEL=claude-3-5-sonnet-20241022

# ---- Retrieval ----
RETRIEVAL_TOP_K=50
RERANK_TOP_N=8
RRF_K=60
BM25_INDEX_DIR=/var/lib/rag/bm25

# ---- Embeddings ----
TEI_ENDPOINT=http://localhost:7100
EMBEDDING_PROVIDER=tei
EMBEDDING_SETUP=dual
HF_TOKEN=

# ---- Langfuse (runtime observability) ----
LANGFUSE_HOST=http://langfuse:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

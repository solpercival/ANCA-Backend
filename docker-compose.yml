# Local stack. `docker compose up` brings the application, data services, and
# Ollama. Observability remains behind a profile.
#
#   docker compose up                          # application + data + Ollama
#   docker compose --profile observability up   # + langfuse
services:
  orchestrator:
    build:
      context: .
      dockerfile: docker/orchestrator.Dockerfile
    env_file: .env
    ports: ["8080:8080"]
    depends_on: 
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      model-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/ready >/dev/null || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 20s

  # One-shot job: writes embeddings to PostgreSQL. Run with `make ingest` after the model profile is up.
  ingestion:
    build:
      context: .
      dockerfile: docker/ingestion.Dockerfile
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      model-init:
        condition: service_completed_successfully

  postgres:
    image: pgvector/pgvector:pg16   # postgres + pgvector extension
    env_file: .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10
    ports: ["5432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scripts/init_pgvector.sql:/docker-entrypoint-initdb.d/init.sql:ro

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    ports: ["6379:6379"]
    volumes: ["redisdata:/data"]

  # --- optional: local models ---
  ollama:
    image: ollama/ollama:latest
    healthcheck:
      # the ollama image ships no curl; use its own CLI, which needs the daemon up
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    ports: ["11434:11434"]
    volumes:
      - ollamadata:/root/.ollama

  model-init:
    image: ollama/ollama:latest
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      OLLAMA_HOST: ollama:11434
    volumes:
      - ollamadata:/root/.ollama
    entrypoint: ["/bin/sh", "-c"]
    command:
      - ollama pull qwen3-embedding:0.6b && ollama pull qwen3:4b

  # --- optional: tei for sparse/SPLADE embeddings (start with: docker compose --profile sparse up) ---
  tei:
    profiles: ["sparse"]
    image: ghcr.io/huggingface/text-embeddings-inference:cpu-1.6
    env_file: .env
    ports: ["7100:80"]
    volumes: ["model-cache:/data"]
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN}
    command: --model-id naver/splade-cocondenser-ensembledistil --pooling splade

  # --- optional: observability ---
  langfuse:
    profiles: ["observability"]
    image: langfuse/langfuse:2
    env_file: .env
    ports: ["3000:3000"]
    depends_on: [postgres]

volumes:
  pgdata:
  redisdata:
  ollamadata:
  model-cache: